{
  "name": "auto-wordpress-blog",
  "version": "1.0.0",
  "description": "Lambda function to post to WordPress",
  "main": "dist/wordpress-post/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "deploy": "npm run build && cp -r node_modules dist && zip -r dist/your-package.zip dist"
  },
  "keywords": [
    "aws",
    "lambda",
    "wordpress"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
} 